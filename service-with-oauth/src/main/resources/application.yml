server.port: 8081
spring:
  security:
    oauth2:
      provider:
        google:
          issuer-uri: https://accounts.google.com
      client:
        registration:
          google:
            client-id: ${client_id}
            client-secret: ${client_secret}


logging:
  level:
    org:
      springframework:
        security: trace
